fragment UserInfo on User {
  id
  firebaseId
  name
  email
  imageBucketKey
  role
  createdAt
  updatedAt
}

query Me {
  me {
    ...UserInfo
  }
}

mutation SignUp($input: SignUpInput!) {
  signUp(input: $input) {
    ...UserInfo
  }
}
